# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

This repository is currently empty scaffolding — no source code, build configuration, or tests exist yet.
The working tree contains only placeholder directories:

- `Backend/` — intended for backend/server code (empty)
- `Frontend/` — intended for frontend/client code (empty)
- `Database/` — intended for database schema/scripts (empty)
- `Requerimientos/` — intended for requirements documentation (empty)
- `Capturadepantalla/` — intended for screenshots (empty)

None of these directories have any content yet, and there is no `package.json`, dependency manifest, or
config file anywhere in the repo to infer a stack, build commands, or test commands from.

## Working in this repo

Since there is no established structure or tooling yet, do not assume a framework, language, or convention —
confirm with the user before scaffolding a stack into `Backend/`, `Frontend/`, or `Database/`. Once real code
lands, update this file with actual build/lint/test commands and the architecture that emerges.
